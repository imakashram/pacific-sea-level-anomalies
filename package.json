{
  "name": "pacific-sea-level-anomalies",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node -e \"const fs = require('fs'); ['package-lock.json', 'yarn.lock'].forEach(f => fs.existsSync(f) && fs.unlinkSync(f)); if (!/pnpm/.test(process.env.npm_config_user_agent || '')) { console.error('Use pnpm instead'); process.exit(1); }\"",
    "dev:api": "pnpm --filter @workspace/api-server dev",
    "dev:story": "pnpm --filter @workspace/pacific-climate-story dev",
    "dev": "pnpm --filter @workspace/api-server --filter @workspace/pacific-climate-story --parallel dev",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:api": "pnpm --filter @workspace/api-server build",
    "build:story": "pnpm --filter @workspace/pacific-climate-story build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
